class NotesListing {
  String noteId;
  String noteTitle;
  DateTime createDateTime;
  DateTime editDateTime;

  NotesListing(
      {this.noteId, this.noteTitle, this.createDateTime, this.editDateTime});
}
